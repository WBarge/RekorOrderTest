import { PendingOrderParser } from './pending-order-parser';

describe('PendingOrderParser', () => {
  it('should create an instance', () => {
    expect(new PendingOrderParser()).toBeTruthy();
  });
});
