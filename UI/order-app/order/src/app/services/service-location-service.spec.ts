import { ServiceLocationService } from './service-location-service';

describe('ServiceLocationService', () => {
  it('should create an instance', () => {
    expect(new ServiceLocationService()).toBeTruthy();
  });
});
