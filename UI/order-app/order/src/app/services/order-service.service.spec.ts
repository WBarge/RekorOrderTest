import { OrderServiceService } from './order-service.service';

describe('OrderService', () => {
  it('should create an instance', () => {
    expect(new OrderServiceService()).toBeTruthy();
  });
});
